{
  "name": "elire",
  "private": true,
  "workspaces": ["apps/*"],
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "dev": "echo 'No workspaces yet — nothing to run'",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "echo 'No workspaces yet — nothing to typecheck'",
    "test": "echo 'No workspaces yet — nothing to test'",
    "build": "echo 'No workspaces yet — nothing to build'",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "lefthook": "^1.8.2",
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0"
  }
}
